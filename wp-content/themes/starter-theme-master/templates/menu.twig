{% if menu %}
	<ul class="menu">
	{% for item in items %}
		<li class="{{ item.classes | join(' ') }}">
			<a target="{{ item.target }}" href="{{ item.link }}">{{ item.title }}</a>
		</li>
	{% endfor %}
		<li><label class="btnCerrar" for="off">&#215;</label></li>
	</ul>
{% endif %}
